#pragma once
#include <string>
#include <vector>

void sortQuick(std::vector<int> &inputArray, int beginOfInputArray, int endOfInputArray);

//Search of the most frequent element in the array.
int search(std::vector<int> &inputArray, int sizeOfInputArray);

//Search of the most frequent element in the file.
//int fileSearch(std::string filePath);